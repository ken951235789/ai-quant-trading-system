# 公開版本發布前安全檢查

檢查日期：2026-09-16

## 檢查範圍

本報告只涵蓋準備公開的獨立快照，不包含私人工作目錄、行情、模型、資料庫、交易紀錄或憑證。公開快照採白名單建立，並使用全新的 Git 歷史。

## 結果

| 檢查 | 結果 |
|---|---|
| 公開快照大小 | 約 2.7 MB |
| Gitleaks 8.30.1 | 未偵測到秘密字串 |
| 本機路徑、非範例 Email、私人 IP | 未偵測到個人值 |
| 模型、資料庫、行情及封存檔副檔名 | 未收錄 |
| Bandit | 0 個高風險；動態 SQL 識別字已人工確認白名單來源 |
| pip-audit | 123 個鎖定依賴，未發現已知漏洞 |
| Ruff | 通過 |
| Pytest | 494 passed、4 subtests passed |

## 已人工確認項目

- `.env.example` 的交易所與 Email 憑證欄位為空白。
- PostgreSQL 與 Grafana 密碼是明確的 `change-me` 占位值，不是部署密碼。
- 測試使用的 Email 均為 `example.com` 保留網域。
- SQLite 動態欄位名稱先通過識別字正規表示式，資料值仍使用參數化查詢。
- 子程序使用參數陣列且未啟用 `shell=True`。

## 限制

本次檢查未發現敏感資料或已知高風險問題，只代表上述工具、規則與檢查日期內的結果；任何掃描都無法保證不存在未知漏洞或無法辨識的敏感資訊。
